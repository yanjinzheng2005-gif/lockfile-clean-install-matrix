# Changelog

## 0.1.0 - Unreleased

- Initial npm and pnpm Linux cold-install matrix candidate.
- Separate baseline/candidate project copies and caches.
- Docker safety boundary, timeout cleanup, protected-file mutation detection, normalized logical dependency inventory, bin comparison, JSON/Markdown receipts, CLI, and GitHub Action.
- Controlled pass, regression, network, timeout, mutation, tree-difference, and credential-redaction tests.
- Structured npm JSON and pnpm YAML preflight, npm shrinkwrap-priority handling, published pnpm security floor, recursive workspace inventory, separate bootstrap/project caches, fail-closed Action default, and real Docker timeout/mutation probes.
- Projectless exact-manager bootstrap so pnpm 12 can install its required native binary without enabling any tested-project lifecycle script.

No public release has been made yet.
